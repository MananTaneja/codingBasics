#include <bits/stdc++.h>

using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n, m;
        cin >> n >> m;
        vector<int> first(m);
        vector<int> second(m);
        for (int i = 0; i < m; i++)
        {
            cin >> first[i] >> second[i];
        }
        if (n == 1)
        {
            return max(first[])
        }
    }

    return 0;
}
