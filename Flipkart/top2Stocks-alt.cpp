#include<bits/stdc++.h>
using namespace std;

int main() {
    
    vector<pair<char, int>> stream = {{'a', 10}, {'b',20}, {'c',30}, {'d', 40}, {'a', 30}, {'d',10}};
    

    for(auto stock: stream) {
        if(s.find(stock.first) == s.end()) {
            s.in
        }
    }

    return 0;
}